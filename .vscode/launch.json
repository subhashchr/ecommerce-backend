{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Go Server (Windows)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}\\main.go",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
